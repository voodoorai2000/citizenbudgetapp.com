- if object.extra?
  = render partial: 'modal', locals: {object: object}
- if object.embed?
  .modal.hide.fade.embed(id="#{object.title.parameterize}_embed")
    = object.embed.html_safe
    = link_to t('.close'), '#', 'data-dismiss' => 'modal', 'class' => 'btn btn-primary'
