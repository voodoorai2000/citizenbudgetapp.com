# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  # Active Admin dashboard
  active_consultations: 'Active consultations'
  future_consultations: 'Future consultations'
  past_consultations: 'Past consultations'

  # Active Admin index
  name: Name
  starts_at: Start date
  ends_at: End date
  consultations: Consultations

  # Active Admin show
  new_section: 'Next: Add the first section to your consultation'
  new_question: 'Next: Add the first service or activity to this section'

  # Active Admin form
  inputs: "%{type} details"
  destroy: Delete

  # Translations in models
  untitled: Untitled
  period: "%{starts_at} to %{ends_at}"
  starting: "starting %{date}"
  ending: "ending %{date}"

  activerecord:
    models: &models
      admin_user: Admin user
  mongoid:
    models: *models

  time:
    formats:
      short: '%B %e, %Y'
      long: '%B %e, %Y at %H:%M'

  errors:
    messages:
      ends_at_must_be_greater_than_starts_at: 'The end time must be after the start time'